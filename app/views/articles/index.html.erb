<p id="notice"><%= notice %></p>

<h1>Noticias!</h1>
<br>
<br>
    <% @articles.each do |article| %>

        <h2><%= link_to article.titulo, article %></h2>
        <span><%= article.created_at %></span>
        <h3><%= article.bajada %></h3>
        <p><%= truncate(article.cuerpo, length: 500, separator: ' ') %></p>
        <p><%= article.comments.count %> comentarios</p>

        <br>

        <span><%= link_to 'Editar', edit_article_path(article) %></span>
        <span><%= link_to 'Eliminar', article, method: :delete, data: { confirm: 'Seguro?' } %></span>
        <br>
        <br>
    <% end %>

<br>
<%= link_to 'Nuevo artículo', new_article_path %>
<br>
<span>Fuente: http://cnnespanol.cnn.com/ </span>
